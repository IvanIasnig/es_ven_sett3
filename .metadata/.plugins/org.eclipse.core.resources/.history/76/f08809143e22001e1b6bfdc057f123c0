package DAO;

import java.util.List;
import java.util.UUID;

import Application.Prestito;
import Application.Utente;

public class PrestitoDAO {

    public void aggiungiPrestito(Prestito prestito) {
        // codice per aggiungere un prestito al database
    }

    public void rimuoviPrestito(UUID id) {
        // codice per rimuovere un prestito dal database
    }

    public Prestito trovaPerId(UUID id) {
        // codice per trovare un prestito per id
    }

    public List<Prestito> trovaPerUtente(Utente utente) {
        // codice per trovare prestiti per un dato utente
    }

    public List<Prestito> trovaPrestitiScadutiENonRestituiti() {
        // codice per trovare prestiti scaduti e non ancora restituiti
    }
}

